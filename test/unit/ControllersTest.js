"use strict";

/* jasmine specs for controllers go here */
define([ "js/app" ], function( app ) {
  describe("test Controllers, add by CUCmehp", function() {
    beforeEach(module("myApp.controllers"));

    it(
      "should ....",
      inject(function() {
        //spec body
        alert("CUCmehp: hello worold");
      })
    );

    it(
      "should ....",
      inject(function() {
        //spec body
      })
    );
  });
});
